[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: marks tests as integration tests (may require Ollama running)
    unit: marks tests as unit tests (no external dependencies)
addopts = -v --tb=short
